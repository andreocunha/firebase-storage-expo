import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        width: "100%",
        height: 80,
        flexDirection: "row",
    },
    titulo : {
        fontSize: 20,
        color: "#FFF",
    }
});